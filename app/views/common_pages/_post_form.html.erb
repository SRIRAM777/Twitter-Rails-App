<div class='post-form'>
	<%= form_for(@post) do |f| %>
    	<div class="field">
      		<%= f.text_area :content, placeholder: "Tweet here..." %>
    	</div>
    	<%= f.submit "Post", class: "btn btn-primary" %>
	<% end %>
</div>