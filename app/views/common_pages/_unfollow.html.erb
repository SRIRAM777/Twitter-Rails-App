<%= form_for(current_user.follower_relations.find_by(followed_by_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
    <%= f.submit "Unfollow", class: "btn" %>
<% end %>